# Profile photo

A placeholder avatar (`waleed.svg`) ships with the project so the site looks
complete out of the box.

To use your real photo:
1. Add your image here, e.g. `waleed.jpg` (portrait, ~4:5 ratio looks best).
2. Update `avatar` in `src/data/profile.ts` to `/profile/waleed.jpg`.
